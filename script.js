let ratedValue = 0;

function rate(value) {
  ratedValue = value;
  // Aqui você pode enviar o valor da avaliação para o servidor ou fazer qualquer outra ação com ele
  console.log("Avaliação: " + ratedValue);
}
